<Window x:Class="SmartRecipe.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Smart Recipe Manager" 
        Height="955" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundGradientBrush}"
        WindowStyle="None"
        AllowsTransparency="True"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Modern Navigation Button Style -->
        <Style x:Key="ModernNavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextWhiteBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               CornerRadius="8"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Window Control Button Style -->
        <Style x:Key="WindowControlButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextWhiteBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="{StaticResource BackgroundSecondaryBrush}">
        <Border.Effect>
            <DropShadowEffect Color="#30000000" BlurRadius="20" ShadowDepth="8" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Enhanced Top Navigation Bar -->
            <Border Grid.Row="0" 
                   Background="{StaticResource PrimaryGradientBrush}" 
                   CornerRadius="12,12,0,0"
                   Height="70"
                   MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="24,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Enhanced Logo and Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Logo Container -->
                        <Border Background="#30FFFFFF"
                               Width="48" Height="48"
                               CornerRadius="12"
                               Margin="0,0,16,0"
                               VerticalAlignment="Center">
                            <TextBlock Text="🍳" 
                                      FontSize="24" 
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>

                        <!-- App Title -->
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Smart Recipe" 
                                      FontSize="18" 
                                      FontWeight="Bold" 
                                      Foreground="{StaticResource TextWhiteBrush}"/>
                            <TextBlock Text="Manager" 
                                      FontSize="14" 
                                      FontWeight="Medium" 
                                      Foreground="#E0FFFFFF"
                                      Margin="0,-2,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Enhanced Navigation Menu -->
                    <StackPanel Grid.Column="1" 
                               Orientation="Horizontal" 
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="{Binding IsAuthenticated, Converter={StaticResource BoolToVisibilityConverter}}">

                        <Button Content="🏠 Dashboard" 
                               Command="{Binding NavigateToDashboardCommand}"
                               Style="{StaticResource ModernNavButtonStyle}"
                               Margin="8,0"/>

                        <Button Content="📚 Recipes" 
                               Command="{Binding NavigateToRecipesCommand}"
                               Style="{StaticResource ModernNavButtonStyle}"
                               Margin="8,0"/>

                        <Button Content="➕ Create Recipe" 
                               Command="{Binding NavigateToCreateRecipeCommand}"
                               Style="{StaticResource ModernNavButtonStyle}"
                               Margin="8,0"/>
                        <Button Content="🌟 Discover Recipes" 
       Command="{Binding NavigateToEnhancedRecipesCommand}"
       Style="{StaticResource ModernNavButtonStyle}"
       Margin="8,0"/>
                    </StackPanel>

                    <!-- Enhanced User Menu -->
                    <StackPanel Grid.Column="2" 
                               Orientation="Horizontal" 
                               VerticalAlignment="Center"
                               Margin="0,0,16,0">

                        <!-- Authenticated User Menu -->
                        <StackPanel Orientation="Horizontal" 
                                   Visibility="{Binding IsAuthenticated, Converter={StaticResource BoolToVisibilityConverter}}">

                            <!-- User Avatar and Name -->
                            <Border Background="#30FFFFFF"
                                   CornerRadius="20"
                                   Padding="12,6"
                                   Margin="0,0,8,0"
                                   Cursor="Hand">
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToProfileCommand}"/>
                                </Border.InputBindings>

                                <StackPanel Orientation="Horizontal">
                                    <Border Background="{StaticResource AccentBrush}"
                                           Width="24" Height="24"
                                           CornerRadius="12"
                                           Margin="0,0,8,0">
                                        <TextBlock Text="👤" 
                                                  FontSize="12"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                    </Border>

                                    <TextBlock Text="{Binding CurrentUser.UserName}" 
                                              Foreground="{StaticResource TextWhiteBrush}"
                                              FontWeight="Medium"
                                              VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <Button Content="🚪" 
                                   Command="{Binding LogoutCommand}"
                                   Style="{StaticResource WindowControlButtonStyle}"
                                   ToolTip="Logout"/>
                        </StackPanel>

                        <!-- Login Button -->
                        <Button Content="🔑 Login" 
                               Command="{Binding NavigateToLoginCommand}"
                               Style="{StaticResource ModernNavButtonStyle}"
                               Visibility="{Binding IsAuthenticated, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                    </StackPanel>

                    <!-- Window Controls -->
                    <StackPanel Grid.Column="3" 
                               Orientation="Horizontal" 
                               VerticalAlignment="Center">

                        <Button Content="−" 
                               Click="MinimizeButton_Click"
                               Style="{StaticResource WindowControlButtonStyle}"
                               Margin="4,0"/>

                        <Button Content="⛶" 
                               Click="MaximizeButton_Click"
                               Style="{StaticResource WindowControlButtonStyle}"
                               Margin="4,0"/>

                        <Button Content="✕" 
                               Click="CloseButton_Click"
                               Margin="4,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource WindowControlButtonStyle}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F44336"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                    </StackPanel>
                </Grid>
            </Border>

            <!-- Enhanced Main Content Area -->
            <Border Grid.Row="1" 
                   Background="{StaticResource BackgroundBrush}"
                   CornerRadius="0,0,12,12">
                <ContentPresenter Content="{Binding CurrentView}" 
                                 Margin="0,8,0,0"/>
            </Border>
        </Grid>
    </Border>
</Window>