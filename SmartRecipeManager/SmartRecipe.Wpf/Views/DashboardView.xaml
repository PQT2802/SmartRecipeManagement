<UserControl x:Class="SmartRecipe.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- Enhanced Card Style -->
        <Style x:Key="DashboardCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardGradientBrush}" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Padding" Value="24" />
            <Setter Property="BorderBrush" Value="{StaticResource OutlineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{Binding Source={StaticResource ShadowLightBrush}, Path=Color}" 
                                     BlurRadius="12" ShadowDepth="4" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Statistic Card Style -->
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="CornerRadius" Value="20" />
            <Setter Property="Padding" Value="24,20" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{Binding Source={StaticResource ShadowMediumBrush}, Path=Color}" 
                                     BlurRadius="16" ShadowDepth="6" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Recipe Item Style -->
        <Style x:Key="RecipeItemStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceVariantBrush}" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{Binding Source={StaticResource ShadowLightBrush}, Path=Color}" 
                                     BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="{Binding Source={StaticResource ShadowMediumBrush}, Path=Color}" 
                                             BlurRadius="12" ShadowDepth="4" Opacity="0.2"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Enhanced Welcome Header -->
            <Border Grid.Row="0" Style="{StaticResource DashboardCardStyle}" Margin="0,0,0,32">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <!-- Time-based Greeting -->
                        <TextBlock x:Name="GreetingText"
                                  Text="{Binding WelcomeMessage}" 
                                  FontSize="32" 
                                  FontWeight="Bold"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  Margin="0,0,0,8"/>

                        <TextBlock Text="What delicious recipe will you create today?" 
                                  FontSize="16"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  Margin="0,0,0,16"/>

                        <!-- Quick Stats -->
                        <StackPanel Orientation="Horizontal">
                            <Border Background="{StaticResource SuccessBrush}"
                                   CornerRadius="12"
                                   Padding="12,6"
                                   Margin="0,0,12,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🏆" FontSize="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="Active Cook" 
                                              FontSize="12"
                                              FontWeight="SemiBold"
                                              Foreground="White"/>
                                </StackPanel>
                            </Border>

                            <Border Background="{StaticResource InfoBrush}"
                                   CornerRadius="12"
                                   Padding="12,6">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⭐" FontSize="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="Recipe Master" 
                                              FontSize="12"
                                              FontWeight="SemiBold"
                                              Foreground="White"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </StackPanel>

                    <!-- Enhanced CTA Button -->
                    <Button Grid.Column="1"
                           VerticalAlignment="Center"
                           Command="{Binding CreateRecipeCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{StaticResource PrimaryGradientBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="24,16"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                   CornerRadius="12"
                                                   Padding="{TemplateBinding Padding}">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBlock Text="➕" FontSize="18" Margin="0,0,8,0"/>
                                                    <TextBlock Text="Create New Recipe" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="RenderTransform">
                                                        <Setter.Value>
                                                            <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="RenderTransform">
                                                        <Setter.Value>
                                                            <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Enhanced Statistics Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Recipes Card -->
                <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <Border Background="{StaticResource PrimaryBrush}"
                               Width="64" Height="64"
                               CornerRadius="32"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16">
                            <TextBlock Text="📚" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <TextBlock Text="{Binding TotalRecipes}" 
                                  FontSize="36" 
                                  FontWeight="Bold"
                                  Foreground="{StaticResource PrimaryBrush}"
                                  HorizontalAlignment="Center"
                                  Margin="0,0,0,4"/>

                        <TextBlock Text="Total Recipes" 
                                  FontSize="14"
                                  FontWeight="Medium"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Total Likes Card -->
                <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <Border Background="{StaticResource AccentBrush}"
                               Width="64" Height="64"
                               CornerRadius="32"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16">
                            <TextBlock Text="❤️" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <TextBlock Text="{Binding TotalLikes}" 
                                  FontSize="36" 
                                  FontWeight="Bold"
                                  Foreground="{StaticResource AccentBrush}"
                                  HorizontalAlignment="Center"
                                  Margin="0,0,0,4"/>

                        <TextBlock Text="Total Likes" 
                                  FontSize="14"
                                  FontWeight="Medium"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Views/Comments Card -->
                <Border Grid.Column="4" Style="{StaticResource StatCardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <Border Background="{StaticResource SecondaryBrush}"
                               Width="64" Height="64"
                               CornerRadius="32"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16">
                            <TextBlock Text="👁️" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <TextBlock Text="1.2K" 
                                  FontSize="36" 
                                  FontWeight="Bold"
                                  Foreground="{StaticResource SecondaryBrush}"
                                  HorizontalAlignment="Center"
                                  Margin="0,0,0,4"/>

                        <TextBlock Text="Total Views" 
                                  FontSize="14"
                                  FontWeight="Medium"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Quick Actions -->
            <Border Grid.Row="2" Style="{StaticResource DashboardCardStyle}" Margin="0,0,0,32">
                <StackPanel>
                    <TextBlock Text="🚀 Quick Actions" 
                              FontSize="20" 
                              FontWeight="Bold"
                              Foreground="{StaticResource TextPrimaryBrush}"
                              Margin="0,0,0,20"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Browse Recipes -->
                        <Button Grid.Column="0" Command="{Binding ViewAllRecipesCommand}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="{StaticResource SurfaceVariantBrush}"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="20,16"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                       CornerRadius="12"
                                                       Padding="{TemplateBinding Padding}">
                                                    <StackPanel HorizontalAlignment="Center">
                                                        <TextBlock Text="🔍" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                                        <TextBlock Text="Browse" FontSize="14" FontWeight="Medium" 
                                                                  Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                                        <Setter Property="TextBlock.Foreground" Value="White"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <!-- Favorites -->
                        <Button Grid.Column="2">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="{StaticResource SurfaceVariantBrush}"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="20,16"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                       CornerRadius="12"
                                                       Padding="{TemplateBinding Padding}">
                                                    <StackPanel HorizontalAlignment="Center">
                                                        <TextBlock Text="💖" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                                        <TextBlock Text="Favorites" FontSize="14" FontWeight="Medium" 
                                                                  Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                                                        <Setter Property="TextBlock.Foreground" Value="White"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <!-- Shopping List -->
                        <Button Grid.Column="4">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="{StaticResource SurfaceVariantBrush}"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="20,16"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                       CornerRadius="12"
                                                       Padding="{TemplateBinding Padding}">
                                                    <StackPanel HorizontalAlignment="Center">
                                                        <TextBlock Text="🛒" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                                        <TextBlock Text="Shopping" FontSize="14" FontWeight="Medium" 
                                                                  Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                                                        <Setter Property="TextBlock.Foreground" Value="White"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <!-- Meal Planner -->
                        <Button Grid.Column="6">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="{StaticResource SurfaceVariantBrush}"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="20,16"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                       CornerRadius="12"
                                                       Padding="{TemplateBinding Padding}">
                                                    <StackPanel HorizontalAlignment="Center">
                                                        <TextBlock Text="📅" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                                        <TextBlock Text="Planner" FontSize="14" FontWeight="Medium" 
                                                                  Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
                                                        <Setter Property="TextBlock.Foreground" Value="White"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Enhanced Recent and Popular Recipes -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Recent Recipes -->
                <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <Grid Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🕒" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Recent Recipes" 
                                          FontSize="20" 
                                          FontWeight="Bold"
                                          Foreground="{StaticResource TextPrimaryBrush}"
                                          VerticalAlignment="Center"/>
                            </StackPanel>

                            <Button Content="View All →" 
                                   Command="{Binding ViewAllRecipesCommand}"
                                   Style="{StaticResource LinkButtonStyle}"
                                   HorizontalAlignment="Right"
                                   FontSize="14"/>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding RecentRecipes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource RecipeItemStyle}">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" 
                                                         Command="{Binding DataContext.ViewRecipeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                         CommandParameter="{Binding}"/>
                                        </Border.InputBindings>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Recipe Image/Icon -->
                                            <Border Grid.Column="0"
                                                   Width="56" Height="56"
                                                   CornerRadius="12"
                                                   Background="{StaticResource PrimaryGradientBrush}"
                                                   Margin="0,0,16,0">
                                                <TextBlock Text="🍽️" 
                                                          FontSize="24"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                            </Border>

                                            <!-- Recipe Info -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Title}" 
                                                          FontWeight="SemiBold"
                                                          FontSize="16"
                                                          Foreground="{StaticResource TextPrimaryBrush}"
                                                          TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding Category}" 
                                                          FontSize="14"
                                                          Foreground="{StaticResource TextSecondaryBrush}"
                                                          Margin="0,2,0,0"/>
                                            </StackPanel>

                                            <!-- Time Badge -->
                                            <Border Grid.Column="2"
                                                   Background="{StaticResource OutlineVariantBrush}"
                                                   CornerRadius="8"
                                                   Padding="8,4"
                                                   VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="⏱️" FontSize="12" Margin="0,0,4,0"/>
                                                    <TextBlock Text="{Binding PrepTimeMinutes}" 
                                                              FontSize="12"
                                                              FontWeight="Medium"
                                                              Foreground="{StaticResource TextSecondaryBrush}"/>
                                                    <TextBlock Text="min" 
                                                              FontSize="12"
                                                              Foreground="{StaticResource TextSecondaryBrush}"
                                                              Margin="2,0,0,0"/>
                                                </StackPanel>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Popular Recipes -->
                <Border Grid.Column="2" Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <Grid Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔥" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Popular Recipes" 
                                          FontSize="20" 
                                          FontWeight="Bold"
                                          Foreground="{StaticResource TextPrimaryBrush}"
                                          VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding PopularRecipes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource RecipeItemStyle}">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" 
                                                         Command="{Binding DataContext.ViewRecipeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                         CommandParameter="{Binding}"/>
                                        </Border.InputBindings>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Recipe Image/Icon -->
                                            <Border Grid.Column="0"
                                                   Width="56" Height="56"
                                                   CornerRadius="12"
                                                   Background="{StaticResource AccentBrush}"
                                                   Margin="0,0,16,0">
                                                <TextBlock Text="🌟" 
                                                          FontSize="24"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                            </Border>

                                            <!-- Recipe Info -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Title}" 
                                                          FontWeight="SemiBold"
                                                          FontSize="16"
                                                          Foreground="{StaticResource TextPrimaryBrush}"
                                                          TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding Category}" 
                                                          FontSize="14"
                                                          Foreground="{StaticResource TextSecondaryBrush}"
                                                          Margin="0,2,0,0"/>
                                            </StackPanel>

                                            <!-- Likes Badge -->
                                            <Border Grid.Column="2"
                                                   Background="{StaticResource ErrorBrush}"
                                                   CornerRadius="12"
                                                   Padding="8,4"
                                                   VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="❤️" FontSize="12" Margin="0,0,4,0"/>
                                                    <TextBlock Text="{Binding LikesCount}" 
                                                              FontSize="12"
                                                              FontWeight="SemiBold"
                                                              Foreground="White"/>
                                                </StackPanel>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Loading Indicator -->
            <Grid Grid.Row="4" 
                  Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Margin="0,40,0,0">
                <Border Background="{StaticResource SurfaceBrush}"
                       CornerRadius="16"
                       Padding="32,24">
                    <StackPanel HorizontalAlignment="Center">
                        <Border Width="48" Height="48"
                               Background="{StaticResource PrimaryBrush}"
                               CornerRadius="24"
                               Margin="0,0,0,16">
                            <TextBlock Text="🍳" 
                                      FontSize="24"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>

                        <ProgressBar Width="120" Height="4" 
                                    IsIndeterminate="True"
                                    Background="{StaticResource OutlineVariantBrush}"
                                    Foreground="{StaticResource PrimaryBrush}"
                                    Margin="0,0,0,12"/>

                        <TextBlock Text="Loading your kitchen..." 
                                  HorizontalAlignment="Center"
                                  FontSize="14"
                                  FontWeight="Medium"
                                  Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>